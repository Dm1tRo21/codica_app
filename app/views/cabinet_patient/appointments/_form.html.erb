<%= form_with(model: [:cabinet_patient, appointment]) do |form| %>
  <% if appointment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(appointment.errors.count, "error") %> prohibited this department from being saved:</h2>

      <ul>
        <% appointment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>



  <div class="row">
    <%= form.label :doctor, class: "col-2 col-form-label", style: "display: block" %>
    <div class="col-10">
      <%= form.collection_select :doctor_id, Doctor.where(department_id: @department_id), :id, :full_name, options = {}, html_options = { class: "form-select" } %>
    </div>
  </div>

  <div class="row">
    <%= form.label :date, class: "col-2 col-form-label", style: "display: block" %>
    <div class="col-10">
      <%= form.datetime_local_field :start_time %>
    </div>
  </div>

  <div>
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
